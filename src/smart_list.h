#ifndef SMART_LIST_HPP_INCLUDED
#define SMART_LIST_HPP_INCLUDED

#include <cstddef>
#include <initializer_list>

namespace smart_list_ {

	template <typename T>
	class smart_list {
	public:
		smart_list(std::size_t count) {
			(void) count;
		}
		// TODO
		// smart_list(T const& one, T const& two, std::size_t count) {}
		// smart_list(std::initializer_list<T> il) {}
		// ... and more

	private:
		// TODO
	};

	template <typename T>
	class very_smart_list {
	public:
		// TODO
	private:
		// TODO
	};

} // namespace smart_list_

#endif
